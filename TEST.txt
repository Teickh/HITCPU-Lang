int target;

int calculate() {
    // This 'target' is completely independent of the global 'target'
    int target = 50; 
    print target;
    return 0;
}

int main() {
    target = 100; // Refers to the global variable
    int result = calculate();
    print target; // Should still print the global value (100)
    return 0;
}